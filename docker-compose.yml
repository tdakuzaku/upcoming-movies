version: '2'
services:
  server:
    container_name: UpcomingMoviesServer
    build: .
    command: npm run dev
    volumes:
      - .:/usr/app/
      - /usr/src/app/node_modules
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: development
      SERVER_PORT: 8080